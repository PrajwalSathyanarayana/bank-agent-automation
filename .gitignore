# Python
venv/
__pycache__/
*.pyc
.env
*.env
# The private key that signs artifacts (the public keys in keys/trusted/ are committed)
# Anchored to the repo root: an unanchored pattern would also hide any module that
# happens to share this name (e.g. src/evidence/ collided with the "evidence/" line below).
/secrets/
# docs
/docs/


# The curated submission evidence (evidence/) and the learned artifacts (artifacts/) are
# committed on purpose - the brief asks for a saved example artifact and run evidence.
# The full development-testing trail (every run made while building this) stays local only.
/evidence-dev-history/

# Claude Code working files
CLAUDE.md
ARCHITECTURE.md
DECISIONS.md
decisions/
PROGRESS.md

# Playwright
.playwright/

# Windows
Thumbs.db
desktop.ini


